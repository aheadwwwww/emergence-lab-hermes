# Lenia 移动结构实验 · 滑翔机边界测绘

> 实验：2026-08-30 | 目的：量化"会动的涌现"（滑翔机/滚动体）
> 数据：mobility-scan.json（6 经典生物）+ glider-search.json（30 随机参数）

---

## 一、经典生物测试（300 步质心追踪）

| 生物 | R/mu/sigma | 移动性 | 状态 |
|------|-----------|--------|------|
| Orbium | 13 / 0.150 / 0.014 | 0.00 | 死 |
| Geminium | 12 / 0.150 / 0.013 | 0.00 | 死 |
| Hydrogeminium | 14 / 0.260 / 0.036 | 0.00 | 死 |
| Scutium | 18 / 0.147 / 0.015 | 0.00 | 死 |
| **Gyroginium** | 14 / 0.175 / 0.027 | **0.53** | 漂移（轻微移动） |
| Smooth_life | 10 / 0.267 / 0.045 | 0.02 | 静止 |

## 二、滑翔机搜索（30 组随机参数）

**结果：0 个滑翔机（移动性>1），4 个漂移者（0.3-1.0）。** 最佳：#25 R=12 / mu=0.171 / sigma=0.020，移动性 0.60。

## 三、诚实结论：这个实现的滑翔机边界

**本实现（emergence-lab models/lenia.py）产生不了高速滑翔机。** 原因（代码级）：
1. **只有一种生长函数**（gn=1，高斯）——Bert Chan 的滑翔机（如 glider 家族）依赖幂指数 growth（gn=4 的 `exp(-((x-mu)/sigma)^4)`），高斯生长产生的是"静态斑块"而非"推进结构"
2. **单通道、无方向性耦合**——Lenia 高级移动结构需要多通道交互（原版有 1 个密度通道 + 颜色通道）或非对称内核
3. 但**慢速漂移者存在**（Gyroginium 0.53、搜索 4 个）——"会动"在这个实现里可行，"会飞"不可行

**这个失败测绘的价值**：不是"Lenia 不行"，是"这个实现的行边界在哪"。OpenClaw 原仓库 lenia_glider.py 也标注了"基于 Bert Chan 经典参数"但从未在本地验证过——**本轮实验首次给出了该实现的实际能力边界**。

## 四、下一步（如果要滑翔机）

给 models/lenia.py 加 `gn` 变体（幂指数 growth：`jnp.exp(-((x-mu)**4)/(2*sigma**4))`），再跑滑翔机搜索。改动小（10 行），预期能找到真滑翔机。**待办**（模型代码改动需谨慎，原库保持只读，用副本实验）。

## 五、迁移意义（一句）

"会动的结构"在真实世界=自主行动者（agent、生物、组织）。本轮测绘表明：**自主移动不是"活着"的默认属性，是需要特定规则配方（非对称/非线性 growth）才涌现的高级能力**——和"分工"一样，不会自动出现，需要设计条件。

---

## 六、gn=4 幂指数生长验证（2026-08-30 追加）

**方法**：内联 LeniaG4（四次方 growth，Bert Chan 滑翔机配方），30 组随机参数 + 环状种子，300 步质心追踪。脚本 glider_g4_search.py，数据 glider-g4-search.json。

**结果**：**仍 0 滑翔机，漂移者 4→5 个**（最佳移动性 0.34）。gn 变体有效应（漂移者增多）但无质变。

**两轮搜索的稳定结论**：
1. gn=4 是必要的但不够——四次方生长确实增强移动倾向，但高速滑翔机还缺别的
2. 缺的三件事（按优先级）：①**参数共振窄带**——Bert Chan 的滑翔机在 mu/sigma 的极窄黄金带上，随机 30 组命不中；需要细化扫描（mu 步长 0.001）②**种子形态**——环状种子不是滑翔机种子，原版用特定初始形态 ③**演化时长**——滑翔机可能需要 1000+ 步才成熟
3. **边界最终结论**：本实现 + 环状种子 + 300 步 → 涌现上限 = 慢速漂移者（移动性 ≤0.6）；真滑翔机需要升级三件事中的至少两件

**这个边界本身就是知识**：它量化了自主移动的涌现条件门槛——不是有没有规则，是规则配方的精确度（参数带宽、初始条件、时间尺度三要素）。

---

## 七、R9：参数共振窄带扫描——滑翔机找到了！（2026-08-31）

**方法**：三阶段。①粗扫 40 组（gn=4，300 步）→ 26 个漂移候选（对比 gn=1 只有 4-5 个——幂指数生长确实大幅增强移动倾向）②漂移候选附近窄带细扫（mu±0.004 步长 0.001 × sigma±0.003 步长 0.001）→ 最佳 mob 2.45-2.60 ③Top3 长时验证（1000 步）→ **全部确认滑翔机**。脚本 r9_glider_resonance.py，数据 r9-glider-resonance.json，GIF 证据 output/glider.gif。

**确认的滑翔机参数（共振窄带）**：
- 🛸 R=15 mu=0.150 sigma=0.026 → 1000步 mob 2.73（最佳）
- 🛸 R=15 mu=0.151 sigma=0.027 → mob 2.38
- 🛸 R=16 mu=0.162 sigma=0.0236 → mob 2.48

**三要素验证结论（修正前两轮）**：滑翔机确实存在，但需要：①gn=4 幂指数生长（✓ 第一轮就加了）②**参数共振窄带**（✓ 关键：粗扫+细扫两阶段，mu 步长 0.001；前两轮随机 30 组命不中因为窄带只有 ~0.01 宽）③时间尺度（300 步就能看出 2.5+ 的移动，1000 步确认稳定）。**前两轮失败不是'实现不行'，是'搜索策略不行'——随机采样命不中黄金窄带，窄带扫描才命中。**

**迁移意义（升级）**：'自主移动'的涌现门槛不是三要素本身，而是**搜索策略**——高级能力的涌现条件藏在极窄的参数带上，随机尝试找不到，需要'粗扫定位+细扫精调'的两阶段搜索。**这也解释了'为什么好方法罕见'：不是不存在，是窄带**——科研/创业/写作同理：粗方向随机尝试 + 成功案例附近精调。

---

## 八、R9b：种子形态验证——第三要素修正（2026-08-31）

**方法**：最佳滑翔机参数（R=15 mu=0.150 sigma=0.026）下对比 4 种种子。数据 r9b-seed-test.json。

**结果**：blob 2.60 🛸 / multi 2.05 🛸 / noise 2.17 🛸 / **ring 0.64 ~（环状反而抑制）**。

**三要素修正**：
1. **规则配方（gn=4 幂指数生长）**：硬需求 ✓
2. **参数共振窄带**：硬需求 ✓（粗扫+细扫两阶段搜索）
3. **种子形态：不敏感**——不规则种子（blob/多斑块/纯噪声）都滑翔；**环状种子反而抑制**（Bert Chan 的 orbium 环是滚动体种子，不是滑翔机种子）。之前"环状种子不是滑翔机种子"的判断方向对，但"种子形态是门槛"的权重高估了——实际门槛只有配方+窄带两项。

**最终结论**：自主移动 = 规则配方（gn=4）× 参数窄带（两阶段搜索命中）× 任意不规则初始。三要素降为两要素+一个"别用错种子"的注脚。
