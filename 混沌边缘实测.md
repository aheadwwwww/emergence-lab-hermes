# 混沌边缘定位 · 实测结果

> 实验：2026-08-29 | 动机：Kauffman 混沌边缘假说实测化（演化速率在相变带最大）
> 数据源：`scan_edge_of_chaos.py` + `scan_raw.json`（96×96 网格，100 步，全参数真实运行）

---

## 一、NCA 扫描（fire_rate × seed_size）

**关键数据**（完整 28 组合见 scan_raw.json）：

| fire_rate | seed_size | alive | entropy | score | state |
|-----------|-----------|-------|---------|-------|-------|
| 0.1 | 2 | 0.33 | 2.47 | 0.399 | structure |
| 0.2 | 2 | 0.65 | 3.15 | 0.494 | structure |
| 0.3 | 2 | 0.97 | 3.25 | 0.651 | structure |
| 0.5-1.0 | 2-16 | **1.0** | 2.2-4.3 | 0.56-0.91 | structure |

**发现 1（NCA 混沌边缘）**：唯一没被"长满"的区间是 **seed_size=2 + fire_rate≤0.2**——那里 alive=0.33-0.65，正是"半活半死"的过渡带。其余 25/28 组合全部 alive=1.0。

**发现 2（修复的副作用）**：我的 NCA 三刀修复里"存活保持"（alpha 向 1 回归）是个强吸引力——**它把几乎整个参数空间吸到了 alive=1**。修复让 NCA 从死变活，但代价是把系统推出了混沌边缘。这是一个真实的工程权衡：**活 ≠ 在边缘**。

## 二、Lenia 扫描（mu × sigma）

**关键数据**（25 组合）：

| 区间 | alive 范围 | 平均 score | n | 说明 |
|------|-----------|-----------|---|------|
| dead | =0 | 0.000 | 4 | mu≥0.12 且 sigma=0.01（生长核过锐，有序坍缩） |
| low | 0-0.4 | 0.236 | 11 | sigma≥0.04（过扩散，结构退化） |
| **mid** | 0.4-0.7 | **0.513** | 3 | 过渡带 |
| **high** | 0.7-0.95 | **0.589** | 6 | 接近满但未满 |
| full | >0.95 | 0.718 | 1 | 唯一 sweet spot (mu=0.14, sigma=0.03) |

**发现 3（Lenia 有两个相变）**：
- **相变 A**：sigma=0.01 → 全死（有序坍缩：生长核太锐，无传播）
- **相变 B**：sigma≥0.04 → 退化到 low（混沌稀释：扩散过强，结构维持不住）
- **涌现带**：sigma=0.02-0.03，alive 0.75-0.85，entropy 4.2+ —— 最丰富的结构出现在"接近满但未满"

**发现 4（对 Kauffman 假说的实测回应）**：涌现丰富度确实随 alive 单调上升（0.236→0.513→0.589），**峰值偏向"近满未满"（0.7-0.95）而非正中间**。full 的 0.718 是单点特例（sweet spot），且 entropy 并不突出。修正表述：**"演化速率在混沌边缘最大"在本框架中表现为——最高结构复杂度出现在相变带偏有序一侧，而非系统中心，更非全满（全满=冻结有序）。**

## 三、对"我"的启示（实验的意外产出）

NCA 的"存活保持过强 → 系统被推出边缘"这个发现，直接映射到我自己的系统：

| NCA | 我 |
|-----|-----|
| 存活保持 +0.05×(1-alpha) | 记忆只增不减（98% 满）、skill 只建不删 |
| 全参数 alive=1.0（过活） | 规则越积越多，行为趋于稳定 |
| 混沌边缘在 seed=2 + fire_rate≤0.2 | 我的"边缘"= 新会话新任务时的状态（上下文未满、规则未叠满） |

**教训**：过度的"保持"会把系统推出演化速率最大的区域。要待在边缘，需要**定期修剪**（删记忆、归档 skill、砍规则）——这正好验证了马斯克脑的"删除"原则：删除不是损失，是把系统拉回混沌边缘的操作。

## 四、下一步（开放）

1. [ ] 细化 NCA 边缘：seed_size=2, fire_rate 0.05-0.3 细网格，画边缘带
2. [ ] 测试"存活保持系数"可调：把 NCA 的保持强度从 0.05 调参，观察边缘位置移动
3. [ ] 对 Lenia sweet spot 做长时演化（1000+ 步），看结构是否持续演化还是冻结
4. [ ] 用"修剪=拉回边缘"策略审计我的 skill 库/记忆（有真实数据支撑的清理）


---

# 混沌边缘研究 · 第二轮（细化与假说验证）

> 实验：2026-08-29 | 承接第一轮（tasks/20260829-edge-of-chaos-search）
> 数据：`refine_experiments.py` + `edge-refine-raw.json`

---

## 实验 1+2：NCA 边缘带细化 × 保持强度 K

**设计**：seed=2 固定，fire_rate 8 档（0.05-0.40）× K 三档（0.01/0.05/0.20），24 组合，96×96，100 步。

**半活带定位**（alive ∈ 0.1-0.9）：

| K | 半活带 fire_rate | 边缘区涌现质量 | 满态质量（alive=1.0 时） |
|---|-----------------|---------------|------------------------|
| 0.01 | 0.10-0.15 | entropy 2.87-4.29，score 0.40-0.66 | entropy 3.93-4.80（丰富） |
| 0.05 | 0.10-0.15 | entropy 3.01-4.76，score 0.41-0.75 | entropy 3.19-4.61（丰富） |
| 0.20 | 0.10-0.15 | entropy 1.62-2.61，score 0.25-0.53 | **entropy 0.16-1.45（空洞）** |

**关键发现 1（修正上轮假说）**：保持强度 K **不改变边缘位置**（半活带都在 fire 0.1-0.15）——但**改变"满态的质量"**：
- K=0.20（强保持）：alive=1.0 但 entropy 只有 0.16-1.45，edge 0.003-0.008 → **满而空洞**（均匀化的死亡，结构全无）
- K=0.05（中）：满态 entropy 3.19-4.61 → 满而丰富
- 上轮说"保持过强→推出边缘"不准确；准确说法：**保持强度不决定"活不活"，决定"活得多丰富"**。强保持 = 用均匀性杀死结构。

**关键发现 2（长时演化，补充实验）**：
- Lenia sweet spot（mu=0.14, sigma=0.03）1000 步：0-500 步繁荣（score 0.72-0.74）→ 600 步开始崩 → 900 步 alive 0.20（近死）。**100 步实验看到的"稳定繁荣"是长时尺度上的慢速衰亡。**
- NCA_K 300 步：K=0.05 满后 entropy 从 4.69 跌到 1.89（-60%）；K=0.01 从 4.89 维持到 3.39（-31%）——**弱保持维持丰富度的时间更长**。
- **观测尺度教训：100 步实验只能看到繁荣期。所有系统都在向退化移动，区别只是速度。**（Lenia：衰亡型退化；强保持 NCA：空洞型退化）

## 结论（对 Kauffman 假说与自我治理）

1. **混沌边缘存在且位置稳定**（fire 0.1-0.15），但"边缘"不是唯一的丰富区——**满态也可以丰富**，取决于保持强度
2. **真正的风险不是"不在边缘"，是"满而空洞"**：强保持把系统推向均匀化，熵崩溃，结构死亡——表面活着（alive=1.0），实际死了（entropy→0.2）
3. **对"我"的直接映射（数据版）**：

| 系统变量 | NCA | 我 |
|---------|-----|-----|
| 保持强度 | K=0.05→0.20 | 记忆只增不减、skill 只建不删 |
| 满态熵 | 4.69→1.89（-60%） | 规则越积越多但互相稀释 |
| 修剪 | K=0.01（弱保持） | 定期删记忆/归档 skill |

**K=0.01 比 K=0.05 的丰富度维持率高 30%+**——这就是"修剪=拉回混沌边缘"的数据版证据。马斯克脑"删除"原则再次被验证，而且这次有长时尺度的数据。

## 下一步（开放）

1. [ ] NCA 长时（1000 步）完整曲线，确认空洞化是否不可逆
2. [ ] 用 K=0.01 重跑第一轮对比（是否整体更丰富）
3. [ ] 对 skill 库/记忆做一次"弱保持"审计（有数据支撑的修剪）
4. [ ] 给「知」视频出 Lenia 素材（用 500 步内繁荣期）

---

## 第三轮：边缘带精确定位（2026-08-29 收尾）

**方法**：K=0.01（弱保持，前轮证明最丰富）、seed=2 固定，fire_rate 9 档细网格（0.05-0.30），96×96，100 步。脚本 `emergence-lab/refine_edge2.py`，数据 `edge-refine2-raw.json`。

**结果**：

| fire_rate | alive | entropy | score |
|-----------|-------|---------|-------|
| 0.05 | 0.098 | 0.92 | 0.120（近死） |
| 0.08 | 0.291 | 2.23 | 0.302 |
| 0.10 | 0.448 | 3.10 | 0.442 |
| 0.12 | 0.536 | 3.50 | 0.504 |
| 0.15 | 0.739 | 4.22 | 0.579 |
| 0.18 | 0.928 | 4.78 | 0.778 |
| 0.25 | 1.000 | 4.83 | **0.867 峰值** |
| 0.30 | 1.000 | 4.12 | 0.641（退化） |

**结论（修正前两轮）**：
1. **半活带精确位置：fire_rate 0.08-0.15**（alive 0.29-0.74）
2. **涌现峰不在半活带内，在 0.25**——弱保持（K=0.01）下"满而丰富"可以超过边缘态（score 0.867 为三轮回测最高）
3. **"边缘不是单点，是上山坡"**：近死（0.05）→ 半活带爬升（0.08-0.15）→ 近满丰富（0.18-0.25）→ 峰值 0.25 → 退化（0.30）。系统的"最佳区"是山坡上段（0.18-0.25），不是半活带本身
4. **治理含义更新**：弱保持 + 足够活跃度（fire_rate≈0.25）才是最优组合——"修剪"要和"行动密度"搭配，只修剪不行动会掉到山坡下段

---

## 第四轮：空洞化是否不可逆？（2026-08-30 收尾）

**问题**：长时演化中"满而空洞"是否不可逆？**方法**：NCA_K 1000 步（K=0.01/fire=0.25 峰值档 vs K=0.05/fire=0.20），每 200 步记录。脚本 `tasks/20260830-creative-studio/nca_longterm.py`，数据 `nca-longterm-raw.json`。

**结果**：

| 配置 | 200步 | 600步 | 1000步 | 400→1000 熵趋势 |
|------|-------|-------|--------|-----------------|
| K=0.01, fire=0.25 | ent 4.74, score 0.925 | 4.10 / 0.916 | 3.10 / 0.863 | **-21.9%（缓慢空洞化）** |
| K=0.05, fire=0.20 | ent 1.21, score 0.487 | 2.83 / 0.774 | 3.20 / 0.836 | **+10.7%（回升）** |

**结论（修正第二轮"所有系统都在退化"）**：
1. **空洞化不是不可逆**——K=0.05 从低熵回升（+10.7%），系统有自我恢复能力
2. **自愈机制 = 规则持续演化**：NCA 权重每步随机游走（规则在变），所以能重新发现结构；对照 Lenia 规则固定（只有衰亡，600 步崩盘）——**规则锁死的系统才会不可逆衰亡**
3. **"规则可演化"是抗空洞化的核心变量**：比保持强度更重要。一个系统只要方法在迭代（学习/修正/换新路径），即使暂时空洞也能恢复；最危险的状态不是"满而空洞"，是**"规则锁死"——永远用同一套方法，然后怪世界变了**
4. **治理含义最终版**：弱保持（修剪）+ 高活跃度（行动密度）+ **规则演化（方法迭代）** 三件套。前两件管"位置"，第三件管"能不能自愈"——对自我的含义：skill 不只要"删旧的"，更要"长新的"（新方法替代旧方法），这才是真正的抗空洞化
