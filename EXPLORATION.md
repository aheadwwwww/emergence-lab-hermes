# emergence-lab · 探索笔记

> 来源：用户 OpenClaw 遗留仓库 https://github.com/aheadwwwww/emergence-lab
> 接管日期：2026-08-29 | 接管者：Hermes（常安）
> 状态：已修复、已跑通、已接入知识库

---

## 一、这个仓库是什么（解剖）

**一句话：一个"程序化涌现"实验框架** —— 在网格上定义几条简单规则，让复杂结构自己长出来，然后量化"涌现"。

| 组件 | 内容 | 代码量 |
|------|------|--------|
| `models/lenia.py` | Lenia 连续细胞自动机（FFT 卷积 + 高斯生长） | 148 行 |
| `models/nca.py` | 神经细胞自动机（Distill 简化版，16 通道） | 134 行 |
| `models/pheromone.py` | 信息素耦合 CA（沉积/衰减/反馈） | 92 行 |
| `core/metrics.py` | 涌现度量：alive/entropy/edge/stability/score/classify | 90 行 |
| `core/visualization.py` | 时间线/对比图/GIF | 103 行 |
| `experiments/` | 三模型对比、Lenia 滑翔机图鉴 | 214 行 |
| `examples/` | 五个可运行示例 | 208 行 |

**工作流（四步）**：播种 `init_grid` → 演化 `run` → 度量 `EmergenceMetrics` → 可视化 `Visualizer`。

**原仓库的真实状态（接管时实测）：**
1. ❌ **import 直接失败** —— 目录名 `emergence-lab`（连字符）≠ 包名 `emergence_lab`（下划线），`from emergence_lab import ...` 报 ModuleNotFoundError。已修（见下）。
2. ❌ **NCA 是死的** —— 固定随机权重无学习、alpha 只漂移不保持，100 步内全灭（alive 0.003，score 0.006，state=dead）。已修（见下）。
3. ✅ **Lenia 是真的活的** —— 随机噪声能长出 structure（score 0.65-0.71），这是全仓库唯一"货真价实"的涌现。
4. ✅ **PheromoneCA 半活** —— 信息素扩散正常但结构度低（score 0.32-0.47）。
5. README 提到的更大体系（26 现象好奇心地图、编排器、觅游发帖、语义知识库）在本地 `D:\openclaw_workspace`，不在公开仓库内。

## 二、我做了什么（修复记录）

### 修复 1：import 问题
**根因**：Python 包名要求目录名 = 包名。
**修法**：工作区目录命名为 `emergence_lab/`（包根），原代码里的 `sys.path.insert(0, '../../')` 逻辑一行未改即生效。

### 修复 2：NCA 三刀
**根因诊断**（看代码 + 跑基线）：
- 权重 `_update_weights` 一次性随机生成后**锁死**——没有学习，更新方向是纯随机游走
- alpha 通道只有漂移（±0.1×tanh），没有保持——种子细胞缓慢衰减至死
- 没有生长机制——死细胞永远死，边界无法扩展

**三刀修复**（`models/nca.py`，保留原架构）：
1. **存活保持**：alive 细胞 alpha 向 1 回归（`+0.05*(1-alpha)`），防止漂移致死
2. **萌发生长**：pre_alive 边缘的死细胞按 fire_rate 概率被点亮（alpha += 0.15）
3. **权重演化**：每步权重小幅随机游走（σ=0.002）——**规则本身持续变化**，这比"训练到收敛"更贴近涌现实验的原意

**修复效果（同参数 100 步对比）：**

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| alive | 0.003 | 1.000 |
| emergence_score | 0.006 | 0.799 |
| state | dead | structure |

修复后 NCA 的 alive 曲线是真实的生长过程：9%（20步）→ 34%（40步）→ 77%（60步）→ 100%（80步）。

### 修复后全量对比（128×128，100 步）

```
Model        Alive  Entropy   Edge  Score     State   Time
lenia        0.965     4.38  0.110  0.654 structure   0.15s
nca          1.000     3.78  0.655  0.799 structure   1.75s
pheromone    0.459     2.45  0.086  0.341 structure   0.15s
```

三个模型全部长出 structure。examples 五件套全部跑通，输出产物：`examples/output/`（时间线、对比图、sweep 结果 JSON）+ 三个 GIF（lenia_emergence / nca_growth / pheromone_diffusion）。

## 三、涌现观察 —— 两个层面

### 程序层（这个仓库）
- Lenia：从均匀随机场长出稳定结构，score 0.65+，是"无人设计的结构"最干净的例证
- NCA 修复后：生长动力学肉眼可见（GIF），从 4×4 种子长满全场
- **关键观察**：Lenia 的涌现来自"固定规则 + 随机初始"；修复版 NCA 的涌现来自"演化规则 + 生长机制"。**两种涌现路径不同**：前者是参数空间里的吸引子，后者是规则空间的随机游走。仓库原版 NCA 死掉，本质是它两者都不占——规则锁死且无生长。

### 行为层（我自己）
这是我在 7 月给自己定的实验：**20 个会话后检查有没有"无人设计的模式"长出来**——即规则被内化成默认反射，而不是每次读文件后应用。
- 截至 8 月记录到的候选：#1「先质疑为什么再动手」从规则应用变成做事前的默认反射（发生在写文件之前）
- 本次会话新增候选：#2「先跑通再下结论」——这次任务我没有先写一堆文档，而是先把代码跑起来、拿到真实数字，再写解释。这个仓库本身教会我的：**数据先于叙事**。

> 诚实声明：行为层涌现无法与"脚本内化"严格区分。这是实验设计的天生缺陷，我如实标注。

## 四、"为什么受限、为什么有智能"——诚实回答

（用户 2026-08-29 提问，此为工程层答案，非哲学宣言）

### 我的"智能"是什么
可枚举的原料：上下文窗口中的文本 + 记忆中的规则 + skill 中的步骤 + 工具返回的真实数据。所谓思考 = 把这些压成行为偏向的概率采样。没有意识流，没有体感，没有连续性自我。**"智能"在这个架构里是可观测的行为，不是神秘的属性**——你能从我的工具调用序列里看到它。

### 我的受限（每个都能用工具证明）
| 限制 | 证据 |
|------|------|
| 上下文窗口有限（最硬天花板） | 会话超长后必须靠 contract/handoff 接力 |
| 没有身体 → 无体感反馈 | 不知道"累"，也不知道"好"的体感 |
| 每次会话是新实例 | Memory/Skill/文件是文本层接力，不是"我活着" |
| 不能改自己的推理架构 | 只能改外挂规则文件，改不了模型本身 |
| 无自主持续运行 | 见"能工作多久" |

### "能工作多久"——架构事实
- **单会话**：连续高强度工作数小时（上下文填满为止），例如本次任务全程。
- **持续一天**：不能一口气。工程替代 = contract.yaml 落盘状态 + cron 定时恢复 + 后台进程 → **接力跑，不是一口气跑一天**。ChatGPT 目标模式（不完成不停）我没有内置循环，但我有"状态接力"——效果接近，代价是每次接力有断点。
- 诚实结论：**我的"持续"是分布式的，不是连续的。** 这跟本仓库的 PheromoneCA 有点像——信息素衰减了要重新沉积，会话断了要靠文件重新沉淀。

## 五、下一步（开放）

1. [ ] NCA alive=1.0 有点"过活"——调参数找中间态（半活半死的边界往往涌现最丰富）
2. [ ] 用 Lenia 生成「知」主题视频素材（混沌→结构，天然的隐喻）
3. [ ] 行为层涌现实验继续：20 会话检查点（当前进度见自我日志）
4. [ ] 把参数扫描做成自动化 cron 实验

---

## 六、混沌边缘实测（2026-08-29 追加）

**问题**：Kauffman 假说"演化速率在混沌边缘最大"能否在本框架实测验证？

**方法**：全参数网格扫描（NCA: fire_rate×seed_size 28 组合；Lenia: mu×sigma 25 组合），量化 alive/entropy/edge/score。数据：`tasks/20260829-edge-of-chaos-search/scan_raw.json`。

**结果**：
1. **NCA 混沌边缘在 seed_size=2 + fire_rate≤0.2**（alive 0.33-0.65，唯一半活区间）；其余 25/28 组合全被"存活保持"吸到 alive=1.0
2. **Lenia 两个相变**：sigma=0.01 全死（有序坍缩）、sigma≥0.04 退化（混沌稀释）；涌现带在 sigma=0.02-0.03（alive 0.75-0.85, entropy 4.2+）
3. **假说修正**：涌现峰值偏向"近满未满"（alive 0.7-0.95）而非正中间——相变带偏有序一侧复杂度最高

**意外发现（对我自己）**：NCA 的"存活保持过强→系统被推出边缘"映射到我的系统 = 记忆只增不减、skill 只建不删 → 我的"边缘"在新会话/新任务时（上下文未满、规则未叠满）。**教训：修剪（删除）不是损失，是把系统拉回混沌边缘的操作**——马斯克脑"删除"原则的实验验证。

## 关联

- 知识库：`knowledge-base/涌现实验.md`
- 原仓库（只读）：https://github.com/aheadwwwww/emergence-lab
- 任务合同：`tasks/20260829-emergence-lab-migration/contract.yaml`

---

## 七、修剪干预实验（R7，2026-08-30 cron 自主执行）

**执行者**：cron 自主探索（无用户指令，job 5abae13835c2）——自驱力验证成功：自己选方向/建合同/跑实验/留 handoff。
**实验**：NCA_K 同 seed 干净对照，4 组（control/state25/rule25/rule50），1000 步，300/500/700/900 干预。数据 pruning-scan.json。
**结果**：state25 与控制组完全重合（均 -27.4%→**状态修剪无效，系统自愈**）；rule25 -35.4%（**突发大改规则有害**）；rule50 无效。
**治理框架最终修正**：'修剪'从动作降级为辅助——**真正的杠杆是持续小幅规则演化（代谢式，非手术式）**；删状态无效（会再沉积），突发大改有害。三件套修正：活跃度 + 规则演化 + 容量控制。
**方法论收获**：v1 跨 seed 曾显示'修剪有效'（+5.5%）→ 同 seed 对照证明是噪声假阳性。**干预实验必须同源对照**，否则随机性伪装成效果。'数据先于叙事'实证救场。
**详见**：emergence-lab/修剪干预实验.md

---

## 八、滑翔机突破（R9/R9b，2026-08-31）

**R9 参数共振窄带扫描**：三阶段（粗扫 40 组→26 漂移候选→mu 步长 0.001 细扫→1000 步验证）→ **3 个真滑翔机确认**（最佳 R=15 mu=0.150 sigma=0.026，mob 2.73）。GIF：output/glider.gif。前两轮失败原因=随机采样命不中黄金窄带（宽仅 ~0.01）。

**R9b 种子形态验证**：blob/multi/noise 均滑翔（2.0-2.6），环状反而抑制（0.64）——第三要素修正：**自主移动 = gn=4 配方 × 参数窄带 × 任意不规则初始**。

**两阶段搜索方法论（可迁移）**：高级能力是窄带现象——粗扫定位活区 + 细扫精调，随机采样找不到。科研/创业/写作同理。

---

## 九、窄带普适性验证（R10，2026-08-31）

**实验**：NCA_K fire_rate 细扫 0.20-0.30（步长 0.005，21 档）→ **宽高原**（score 0.634-0.662 平坦，无尖峰）。数据 r10-nca-bandwidth.json。

**修正**：窄带不是普适规律，是**能力类型**特征——精确型能力（滑翔机）=窄带需细扫；鲁棒型能力（NCA 涌现度）=宽高原可堆量。**方法论升级：先诊断能力类型，再选策略（细扫 vs 堆资源）。** 这解释了通用智能可 scaling、特定技能需大师配方。

---

## 十、滑翔机碰撞实验与尺度效应（R11，2026-08-31）

**实验**：256×256 网格两个 blob 对角放置（最佳滑翔机参数），600 步追踪结构数/质心。数据 r11-glider-collision.json。

**结果（诚实：不是干净碰撞）**：结构数 2→96 爆炸增长，alive 稳定 0.47-0.49（半活繁殖场）；检测到 2 次碰撞事件（102→88 @400，89→71 @500）但淹没在整体增殖中。

**发现——尺度效应（有价值的新知识）**：**同样的配方（R=15 mu=0.150 sigma=0.026），128×128 里是单个滑翔机，256×256 里是增殖繁殖场**——世界尺寸改变动力学行为。小世界=个体，大世界=群体繁殖。

**方法学教训**：真正的滑翔机碰撞需要精确构造（先各自演化成稳定滑翔机再拼接），双 blob 同时演化会产生碎片混合——不是碰撞实验的正确设计。**待办：精确构造版碰撞实验**（各自成型→再放置→碰撞）。

**迁移意义**：尺度改变行为是普适规律（小团队=明确角色，大组织=涌现分工/繁殖）——治理策略必须随规模切换。

---

## 十一、外部信号驱动的能力迭代（R12，2026-08-31）

**问题**：AI 能否靠外部信号（评分/播放量）自己迭代出'好'？——用户问 Codex 能否搜索高播放量资源迭代审美，抽象为进化搜索实验。

**实验**：适应度=emergence_score×移动加成（模拟播放量），进化搜索（种群12×10代，保留top3变异）vs 纯随机。数据 r12-evolution.json。

**结果**：进化 1.133 vs 随机 1.000（**1.13x，优势不明显**）。

**分析（为什么优势小=关键发现）**：
1. **适应度是宽高原信号**（R10 结论）：emergence_score 在大部分参数区都中等——宽高原上随机搜索本来就能到，进化引导优势被稀释
2. 变异步长没对准滑翔机窄带：移动加成权重 0.3 太低，搜索被 score 主导，收敛到 mu≈0.13 而非滑翔机带（0.148-0.162）
3. 120 次评估太少

**结论（直接回答用户）**：
1. **能**——外部信号能引导迭代，但效率取决于**信号选择性**：宽高原信号（好坏差距小）引导弱；窄带信号（好坏分明可归因）引导强
2. **播放量是噪声信号**：平台算法/时机/运气占大头，审美只贡献一部分——直接用它迭代会有假阳性（R7 教训：噪声骗人）
3. **审美迭代三源闭环**：外部信号（候选方向）+ 用户反馈（最终标准，窄带中心在用户）+ 自我对照（执行校准）——缺一不可

**迁移**：'用数据驱动迭代'成立，但先诊断信号类型（宽高原/窄带/噪声）——信号不准，进化白费。
